interface:
  display_name: "LEAF Press"
  short_description: "Create citable LEAF Markdown digests"
  default_prompt: "Use $leaf-press to write a status abstract and create a citable .leaf/04-pressed/{slug}.md digest."

policy:
  allow_implicit_invocation: true
