interface:
  display_name: "LEAF Work"
  short_description: "Turn vague intent into reliable work"
  default_prompt: "Use $leaf-work to follow LEAF: learn the context, test one cheap example, architect the result, and feed review back into the next iteration."

policy:
  allow_implicit_invocation: true
