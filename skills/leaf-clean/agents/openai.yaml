interface:
  display_name: "LEAF Clean"
  short_description: "Complete, press, fall, and migrate the .leaf workspace"
  default_prompt: "Use $leaf-clean to tend the .leaf workspace: move ⑧-passed sprouts into leaves, press reference-worthy leaves into citable pressed.md digests, move non-reference-worthy work into fallen, or migrate old layouts reported by leaf doctor."

policy:
  allow_implicit_invocation: true
