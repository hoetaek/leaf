interface:
  display_name: "LEAF Clean"
  short_description: "Clean LEAF documents into simple current reports"
  default_prompt: "Use $leaf-clean to clean LEAF documents: remove stale options, compress process chatter, clarify why/what/evidence/decision, and judge with a Feynman-like simplicity standard."

policy:
  allow_implicit_invocation: true
