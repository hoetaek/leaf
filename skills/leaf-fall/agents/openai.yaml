interface:
  display_name: "LEAF Fall"
  short_description: "Move non-reference LEAF work to fallen"
  default_prompt: "Use $leaf-fall to move LEAF work into fallen with an explicit fallen reason and a useful closure note."

policy:
  allow_implicit_invocation: true
