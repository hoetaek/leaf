interface:
  display_name: "LEAF Fall"
  short_description: "Archive committed leaves after closure"
  default_prompt: "Use $leaf-fall to close an active .leaf leaf into .leaf/fallen while preserving context and writing a useful closure note."

policy:
  allow_implicit_invocation: true
