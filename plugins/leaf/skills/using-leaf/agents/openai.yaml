interface:
  display_name: "Using LEAF"
  short_description: "Route direct, fast-track, or discovery-heavy LEAF work"
  default_prompt: "Use $leaf:using-leaf and follow its SKILL.md canonical direct exclusions and route order. Use soul first for actual LEAF work and preserve the canonical status handoff, approvals, gates, and verification."

policy:
  allow_implicit_invocation: true
