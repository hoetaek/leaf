interface:
  display_name: "Using LEAF"
  short_description: "Entry/router for the LEAF workflow and which leaf skill to use"
  default_prompt: "Use $leaf:using-leaf to orient in the LEAF workflow: the Learn→Example→Architect→Feedback loop, which leaf skill to invoke (soul first for conduct, learn to capture/learn, work to build), and that the `leaf` CLI gives the repo-local .leaf/ workspace."

policy:
  allow_implicit_invocation: true
