interface:
  display_name: "LEAF Polish"
  short_description: "Turn LEAF drafts into current reports"
  default_prompt: "Use $leaf:polish at a real LEAF document boundary to turn the cumulative gate/status report into a simple current report; never delay execution-ready first evidence for it."

policy:
  allow_implicit_invocation: true
