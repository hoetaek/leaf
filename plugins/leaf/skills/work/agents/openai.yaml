interface:
  display_name: "LEAF Work"
  short_description: "Turn vague intent into reliable work"
  default_prompt: "Use $leaf:work. For execution-ready implementation, check the five conditions and make failing-test/reproduction/measurement evidence before LEAF documents; otherwise start after Learn. Preserve hard stops, then record only needed gates, verify, review, retrospect, and keep/press/fall."

policy:
  allow_implicit_invocation: true
