interface:
  display_name: "LEAF Work"
  short_description: "Carry direct, fast-track, or full LEAF work"
  default_prompt: "Use $leaf:work. Keep execution-ready implementation direct. For fast-track LEAF, consume the request-scoped procedure budget while preserving every gate, fold condition, hard stop, review, retrospect, and verification. Otherwise run normal discovery-heavy Work."

policy:
  allow_implicit_invocation: true
