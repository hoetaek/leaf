interface:
  display_name: "LEAF Work"
  short_description: "Carry direct, fast-track, or full LEAF work"
  default_prompt: "Use $leaf:work only after $leaf:using-leaf provides the canonical route and status handoff. Follow that selected route while preserving gates, fold conditions, hard stops, review, retrospect, and verification."

policy:
  allow_implicit_invocation: true
