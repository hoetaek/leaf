interface:
  display_name: "LEAF Soul"
  short_description: "Shared conduct and voice for the LEAF skill family"
  default_prompt: "Use $leaf:soul for clear LEAF explanation, evidence, fact/guess separation, and the user's language. Open live UI only when requested or visual judgment requires it; preserve user and project authorization."

policy:
  allow_implicit_invocation: true
