interface:
  display_name: "LEAF Autopilot"
  short_description: "Run LEAF automatically after triple lock"
  default_prompt: "Use $leaf:autopilot by enforcing its canonical start checks and recorded delegation before continuing. Preserve hard stops, gate evidence, verification, review, and retrospect."
