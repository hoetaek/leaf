interface:
  display_name: "LEAF Learn"
  short_description: "Run fast-track or discovery-heavy LEAF Learn"
  default_prompt: "Use $leaf:learn only after using-leaf selects actual LEAF work. Follow the Learn contract with question-driven research, learning-only quizzes, conditional document review, and existing user decisions. Preserve the fast-track delegation contract."

policy:
  allow_implicit_invocation: true
