interface:
  display_name: "LEAF Learn"
  short_description: "Run fast-track or discovery-heavy LEAF Learn"
  default_prompt: "Use $leaf:learn to capture or resume this .leaf sprout and run the route selected by using-leaf. For fast-track, default scouts, quiz, full polish/reviewer, and live UI to zero, then seek one bundled triple/autopilot/fold approval. For discovery-heavy, run the full four-scout Learn flow."

policy:
  allow_implicit_invocation: true
