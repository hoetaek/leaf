name: validate-plugin

on:
  pull_request:
    paths:
      - ".claude-plugin/**"
      - ".agents/plugins/**"
      - "plugins/leaf/**"
      - "scripts/validate-manifests.mjs"
      - "scripts/eval-execution-ready.mjs"
      - "tests/fixtures/execution-ready-regression.md"
      - "CHANGELOG.md"
      - "scripts/lint-shell.sh"
      - "Cargo.toml"
  push:
    branches: [develop, main]

jobs:
  validate-manifests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
      - run: node scripts/validate-manifests.mjs --audit
      - run: node scripts/eval-execution-ready.mjs

  lint-shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: sudo apt-get update && sudo apt-get install -y shellcheck
      - run: scripts/lint-shell.sh
